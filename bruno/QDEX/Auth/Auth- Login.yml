info:
  name: "Auth: Login"
  type: http
  seq: 1

http:
  method: POST
  url: "{{qdex_v2_base_url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
          "username": "admin",
          "password": "pw@12345"
      }

runtime:
  variables:
    - name: qdex_v2_base_url
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
