info:
  name: "User: All"
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/users
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{qdex_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
