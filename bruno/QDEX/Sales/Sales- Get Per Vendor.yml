info:
  name: "Sales: Get Per Vendor"
  type: http
  seq: 3

http:
  method: GET
  url: "{{qdex_v2_base_url}}/api/v1/sales/vendor/:code"
  params:
    - name: code
      value: "1234"
      type: path
  auth:
    type: bearer
    token: "{{qdex_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
