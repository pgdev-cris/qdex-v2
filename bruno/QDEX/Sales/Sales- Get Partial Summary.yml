info:
  name: "Sales: Get Partial Summary"
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5173/api/v1/remittance/partial-summary/11460
  auth:
    type: bearer
    token: "{{qdex_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
