info:
  name: "Supplier: Get All"
  type: http
  seq: 1

http:
  method: GET
  url: "{{qdex_v2_base_url}}/api/v1/suppliers"
  auth:
    type: bearer
    token: "{{qdex_auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
